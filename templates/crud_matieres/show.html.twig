{% extends 'base_admin.html.twig' %}

{% block title %}Matieres{% endblock %}

{% block body %}
<div>
    <h1>Matieres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ matiere.titre }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ matiere.specialite }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ matiere.niveau }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ matiere.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_matieres_index') }}">back to list</a>

    <a href="{{ path('app_crud_matieres_edit', {'id': matiere.id}) }}">edit</a>

    {{ include('crud_matieres/_delete_form.html.twig') }}
</div>
{% endblock %}
